# QgisServerProjectReloader

Auto reload project based on QgsProject.lastModifiedTime()

This plugin is deprecated.

Now we can use:
```
QGIS_SERVER_PROJECT_CACHE_STRATEGY=periodic
QGIS_SERVER_PROJECT_CACHE_CHECK_INTERVAL=...
```
